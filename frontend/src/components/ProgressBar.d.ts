import { type Stage } from "../stages/data";
interface Props {
    stage: Stage;
    stepIdx: number;
}
export declare function ProgressBar({ stage, stepIdx }: Props): import("react/jsx-runtime").JSX.Element;
export {};
