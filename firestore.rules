rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // usage 등 모든 컬렉션은 클라이언트 직접 접근을 금지한다.
    // 사용량 기록은 Functions(admin SDK)만 수행하며 admin SDK 는 보안 규칙을 우회한다.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
